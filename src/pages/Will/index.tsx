import { ScrollView, View, Button, StyleSheet} from 'react-native';
import React from 'react';

const Will = ({ navigation }) => {
    return (
        <View>
            
        </View>
    );
};
export default Will;
const styles = StyleSheet.create({

}); 